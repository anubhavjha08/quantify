import { History } from 'lucide-react';
import React from 'react'

const Header = () => {
    return (
        <nav>

            <div>
                <button>
                    <History/>
                </button>
            </div>

            <div></div>

        </nav>
    );
}

export default Header